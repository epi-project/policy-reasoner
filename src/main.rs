// Note: might not need, just here to keep Cargo happy. If you don't want a general package but only "libraries", let me know, then we can create a virtual workspace.
fn main() {
    println!("Hello, world!");
}
